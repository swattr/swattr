<h1><%= Swattr.t(:list_resolutions) %></h1>

<table>
  <thead>
    <tr>
      <th><%= Swattr.t(:name) %></th>
      <th><%= Swattr.t(:description) %></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%= render @resolutions %>
  </tbody>
</table>

<br>

<% if policy(@resolutions).new? %>
  <%= link_to Swattr.t(:new_resolution), new_resolution_path %>
<% end %>
